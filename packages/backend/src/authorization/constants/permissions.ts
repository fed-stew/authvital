/**
 * Re-export permissions from shared package
 *
 * @see @authvital/shared for canonical definitions
 */
export {
  TENANT_PERMISSIONS,
  type TenantPermission,
  OWNER_PERMISSIONS,
  ADMIN_PERMISSIONS,
  MEMBER_PERMISSIONS,
} from '@authvital/shared';
