/**
 * Constants Barrel Export
 *
 * All constants are re-exported here for convenient imports:
 *
 * @example
 * ```ts
 * import { TENANT_PERMISSIONS, DEFAULT_TENANT_ROLES } from '@authvital/shared';
 * // or
 * import { hasTenantPermission } from '@authvital/shared/constants';
 * ```
 */

export * from './permissions.js';
export * from './default-roles.js';
